using UnityEngine;

public class ChessListener : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }
    public void setState(string state)
    {
        
    }
}
